[Unit]
Description=Keep Vibecodium reachable while idle

[Service]
ExecStart=/usr/bin/systemd-inhibit --what=sleep --why="vibecodium reachable" --mode=block /usr/bin/sleep infinity
Restart=always

[Install]
WantedBy=default.target
